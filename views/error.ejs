<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Error</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;700&display=swap" rel="stylesheet">
    <style>
        .error-page {
            font-family: 'Nunito', sans-serif;
            background: linear-gradient(270deg, #FF5252, #FF1744, #D50000, #B71C1C);
            background-size: 800% 800%;
            animation: gradientFlow 15s ease infinite;
        }

        @keyframes gradientFlow {
            0% {
                background-position: 0% 50%;
            }

            50% {
                background-position: 100% 50%;
            }

            100% {
                background-position: 0% 50%;
            }
        }

        .glass {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
    </style>
</head>

<body class="error-page min-h-screen flex items-center justify-center text-white">
    <section class="glass rounded-2xl p-10 w-full max-w-md shadow-xl animate-fade-in text-center">
        <div class="text-9xl font-bold mb-4">
            <i class="fas fa-exclamation-circle"></i>
        </div>
        <h1 class="text-4xl font-bold mb-4">Something Went Wrong</h1>
        <p class="text-xl mb-8"><%= error || 'An unexpected error occurred' %></p>
        <div class="flex justify-center gap-4">
            <a href="/"
                class="inline-block bg-white text-red-600 font-semibold px-6 py-3 rounded-lg hover:bg-red-100 transition">
                <i class="fas fa-home"></i> Go Home
            </a>
            <a href="/read"
                class="inline-block bg-white text-red-600 font-semibold px-6 py-3 rounded-lg hover:bg-red-100 transition">
                <i class="fas fa-users"></i> View Users
            </a>
        </div>
    </section>

    <script>
        document.querySelector('section').classList.add('animate-fade-in');
    </script>
</body>

</html>
